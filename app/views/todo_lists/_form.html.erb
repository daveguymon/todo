<%= form_with(model: todo_list, local: true) do |form| %>
  <% if todo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      <ul>
        <% todo_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title, :placeholder => "Name your new list." %>
  </div>

  <div class="field">
    <%= form.text_area :description, :placeholder => "Add a description." %>
  </div>

  <div class="button-container btn-column">
    <div class="button new-object-container">
      <%= form.submit submit_text %>
    </div>
      <%= link_to todo_lists_path, :class => "button new-object-container" do %>
        <div>BACK</div>
      <% end %>
  </div>

<% end %>
